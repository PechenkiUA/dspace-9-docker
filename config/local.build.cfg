# Build-time config only — used during "ant fresh_install" in Dockerfile.backend.
# Real connection settings are generated at container startup by backend-entrypoint.sh.

dspace.dir = /dspace
dspace.server.url = http://localhost:8080/server
dspace.ui.url    = http://localhost:4000

db.url      = jdbc:postgresql://localhost:5432/dspace
db.username = dspace
db.password = dspace
